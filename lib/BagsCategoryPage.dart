import 'package:flutter/material.dart';

class BagsCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم الحقائب')),
      body: Center(child: Text('مرحبا بك في قسم الحقائب')),
    );
  }
}
