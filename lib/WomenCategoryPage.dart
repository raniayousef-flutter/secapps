import 'package:flutter/material.dart';

class WomenCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم النساء')),
      body: Center(child: Text('مرحبا بك في قسم النساء')),
    );
  }
}
