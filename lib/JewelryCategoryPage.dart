import 'package:flutter/material.dart';

class JewelryCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم المجوهرات')),
      body: Center(child: Text('مرحبا بك في قسم المجوهرات والاكسسوارات')),
    );
  }
}
