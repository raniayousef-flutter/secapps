import 'package:flutter/material.dart';

class LingerieCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم اللانجري')),
      body: Center(child: Text('مرحبا بك في قسم اللانجري')),
    );
  }
}
