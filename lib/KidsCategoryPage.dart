import 'package:flutter/material.dart';

class KidsCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم الأطفال')),
      body: Center(child: Text('مرحبا بك في قسم الأطفال')),
    );
  }
}
