import 'package:flutter/material.dart';

class ToysCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم الألعاب')),
      body: Center(child: Text('مرحبا بك في قسم الألعاب')),
    );
  }
}
