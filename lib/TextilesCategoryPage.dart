import 'package:flutter/material.dart';

class TextilesCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم المنسوجات')),
      body: Center(child: Text('مرحبا بك في قسم المنسوجات المنزلية')),
    );
  }
}
