import 'package:flutter/material.dart';

class CarsCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم السيارات')),
      body: Center(child: Text('مرحبا بك في قسم السيارات')),
    );
  }
}
