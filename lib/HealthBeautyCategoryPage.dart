import 'package:flutter/material.dart';

class HealthBeautyCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم الصحة والجمال')),
      body: Center(child: Text('مرحبا بك في قسم الصحة والجمال')),
    );
  }
}
