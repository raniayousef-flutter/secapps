import 'package:flutter/material.dart';

class DevicesCategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('قسم الأجهزة')),
      body: Center(child: Text('مرحبا بك في قسم الأجهزة المنزلية')),
    );
  }
}
